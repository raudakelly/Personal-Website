int start;
int end;
int inc;
int current;
int c;
int k;
read(start, end, inc);
current = start;
while(current <= end) {
  c = current - 32;
  c = c * 5/9;
  k = current + 460;
  k = k * 5/9;
  print(current, c, k);
  printline;
  current = current + inc;
}
